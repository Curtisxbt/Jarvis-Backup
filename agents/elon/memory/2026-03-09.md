# 2026-03-09

- OpenClaw a été mis à jour de `2026.3.7` vers `2026.3.8`.
- La config OpenClaw a été corrigée pour GPT-5.4 avec `agents.defaults.contextTokens = 1000000` dans `~/.openclaw/openclaw.json` ; le status Telegram a ensuite confirmé `Context: ... /1.0m` sur la session principale.
- Important : les anciennes sessions/runs historiques peuvent encore afficher `200000` car elles ont été créées avant le changement ; les nouvelles sessions utilisent désormais `1,000,000`.
- Mission Control a été rendu accessible via Tailscale : Tailscale installé/reconnecté sur Jarvis, IP Tailscale confirmée `100.90.34.65`, puis Mission Control relancé sur `0.0.0.0:3005`.
- Mission Control est désormais géré proprement via PM2 sous le nom `mission-control` ; méthode de lancement manuelle supprimée au profit d’une seule méthode d’exploitation.
- Dans Mission Control, ajout/fix des éléments suivants : mode clair/sombre avec bouton de bascule, favicon custom, portraits photo ronds pour Elon/Jocko dans le Bureau avec anneau d’état vert/rouge, correction du dark mode (ex. bouton `Créer une revue runtime`), affichage PM2 dans l’onglet Équipe, affichage des crons Linux perso dans Calendrier (sans les crons système Ubuntu), vue `Hier` dans Calendrier.
- Le gros incident CPU / ventilos venait de la couche live/SSE ajoutée à Mission Control : le flux `/api/team?stream=1` restait actif après fermeture et bouclait avec erreurs `Controller is already closed`; la couche live a été retirée pour stabiliser la machine.
- L’onglet Calendrier de Mission Control a été optimisé : suppression du double appel à `getCronJobs()`, retrait du chargement des runs détaillés au premier affichage, puis ajout d’un mini cache serveur de 10 secondes ; gain mesuré important sur les changements d’onglet rapides.
- Les crons OpenClaw de clôture mémoire ont été corrigés : Elon `23:30` et Jocko `23:35` font désormais une passe plus courte et doivent écrire au minimum une clôture quotidienne même si rien de substantiel n’est retenu.
- Le backup GitHub a été rationalisé : supprimé du cron Elon puis supprimé aussi du cron Jocko ; il reste uniquement le backup Linux quotidien à `03:00` via `/bin/bash /home/jarvis/backup_cerveau.sh`.
- Cause racine identifiée pour la pollution mémoire après `/compact` : un cron Linux `23:55` lançait `scripts/merge_session_memory.py`, qui importait brutalement des résumés/transcripts de session (`Session importée depuis ...`, `Conversation info`, etc.) dans la mémoire agent.
- Correctif appliqué : suppression du cron Linux `23:55` et suppression du script `scripts/merge_session_memory.py`.
- Le fichier pollué `agents/elon/memory/2026-03-08.md` a été nettoyé manuellement pour retirer tous les blocs de transcript/import de session et ne garder que la mémoire utile.
- Diagnostic des crons de clôture d’hier : le backup `bash ~/backup_cerveau.sh` prend seulement ~`1.62 s`; la vraie cause des durées excessives était la surconsommation de contexte par les prompts, surtout côté Jocko (~118k input tokens pour conclure qu’il n’y avait rien à écrire).
- Les prompts des crons Elon/Jocko ont donc été resserrés pour éviter l’exploration large et réduire le coût des clôtures futures.
- Clôture quotidienne Elon effectuée à `23:30` : passe courte réalisée sur les signaux évidents déjà accessibles ; aucun nouveau point substantiel supplémentaire n’a émergé au-delà des éléments déjà consignés ci-dessus.
