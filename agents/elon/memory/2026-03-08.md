# 2026-03-08

- Mise à jour OpenClaw effectuée vers `2026.3.7` ; gateway redémarré avec succès et runtime confirmé actif.
- Après update, test isolé subagent sur `openai-codex/gpt-5.4` avec tâche `Réponds exactement MODEL_OK.` : succès (`MODEL_OK`). Conclusion : GPT-5.4 est désormais accepté par le runtime.
- Avant ce succès, plusieurs tests GPT-5.4 échouaient en `Unknown model`; le déblocage semble donc lié à l’update `2026.3.7`.
- Denis souhaite basculer le modèle principal sur GPT-5.4. La commande `openclaw models set openai-codex/gpt-5.4` a bien modifié `~/.openclaw/openclaw.json` et affiché `Default model: openai-codex/gpt-5.4`.
- La tentative simultanée de `openclaw gateway restart` a été interrompue (`SIGTERM` côté exécution outil), donc point à revérifier après compact : confirmer que le gateway a bien redémarré après la bascule modèle et que la session principale tourne réellement sur GPT-5.4.
- Gros chantier précédent sur les modèles locaux Ollama : installation puis suppression complète de `qwen3.5:4b`, `qwen3.5:0.8b` et `tinyllama:latest`.
- Toutes les références aux modèles locaux/Ollama ont été supprimées des configs OpenClaw vérifiées (`~/.openclaw/openclaw.json`, `~/.openclaw/agents/main/agent/models.json`, `~/.openclaw/agents/jocko/agent/models.json`).
- Diagnostic Ollama/subagents établi : `ollama/qwen3.5:4b` fonctionnait en direct via Ollama mais échouait/timeoutait dans les sous-agents OpenClaw ; piste retenue = problème d’orchestration OpenClaw+Ollama plutôt qu’un simple problème de modèle local.
- Réglage STT local déjà remis sur Whisper `small` précédemment, car `medium` faisait timeout sur les vocaux courts ; la config audio locale utilisable est donc revenue sur `small`.
- Denis voulait surtout éviter tout bricolage risqué pour GPT-5.4 ; préférence pour approche safe, test isolé d’abord, puis bascule globale seulement après preuve d’exécution réelle.
- Mise en place d’un cron OpenClaw quotidien pour Elon : `elon-2330-memory-sync` (`d8e1289f-dc28-49d5-8d0b-3af8e486f069`), programmé à `23:30` Europe/Paris, session isolée, silencieuse, chargé de clôturer la mémoire Elon puis d’exécuter `bash ~/backup_cerveau.sh`.
- Un effet de bord shell a déclenché une exécution immédiate du script de backup pendant la création du cron Elon ; le payload a ensuite été corrigé proprement pour éviter toute exécution prématurée à l’avenir.
- Denis a ensuite fait configurer aussi Jocko avec un cron distinct `jocko-2330-memory-sync` ; côté système, le job observé est `jocko-2330-memory-sync` (`ec198d6e-b40d-4b3a-9119-356b149d146b`) planifié à `23:35` Europe/Paris et finalisant aussi par `bash ~/backup_cerveau.sh`.
- Mise en place et vérification du Browser Relay Chrome OpenClaw : procédure validée via `openclaw browser extension install` puis `openclaw browser extension path`, chargement manuel de l’extension Chrome en mode développeur, config locale du port `18792` et récupération du gateway token dans `~/.openclaw/openclaw.json`.
- Une fois le Browser Relay connecté, accès confirmé au Chrome de Denis puis à son compte X/Twitter connecté ; le problème initial venait d’un onglet non attaché ou mal ciblé, pas d’un échec global du relay.
- Gros chantier Mission Control lancé dans `/home/jarvis/.openclaw/workspace/mission-control/` avec accord explicite de Denis ; règle clarifiée ensuite : toute l’interface et tous les développements futurs de Mission Control doivent être en français.
- Sprint 1 Mission Control exécuté et validé : socle Next.js local-first, module Mémoire branché sur les vrais fichiers `agents/*`, Calendrier branché sur les vrais cron OpenClaw, Tâches v2 avec édition/archivage/filtres, Vue d’ensemble utile, début de liens entre modules.
- Sprint 2 exécuté et validé : liens navigables entre modules, Mémoire ↔ tâches, Calendrier ↔ tâches, panneau d’alertes sur la home, amélioration de la cohérence inter-modules et du polish UX.
- Sprint 3 exécuté et validé : deep-linking plus précis (`taskId`, `docId`, `jobId`), alertes actionnables, page Équipe enrichie par charge/cron/mémoire, Bureau transformé en radar plus vivant, amélioration nette de la sensation produit.
- Sprint 4 exécuté et validé : confirmation que `openclaw cron run <id>` est disponible, ajout d’une API locale `mission-control/app/api/cron/run/route.ts`, bouton `Run now` sur les jobs cron, création rapide de tâches liées depuis Mémoire et Calendrier, moteur d’alertes dédié (`lib/alerts/engine.ts`) et premiers signaux runtime OpenClaw (`lib/agents/runtime.ts`) injectés dans la home, Équipe et Bureau.
- État produit actuel : Mission Control est désormais un cockpit opérationnel crédible (lecture + corrélation + navigation + alertes + premières actions), mais pas encore final ; prochaine suite logique identifiée avec Denis = Sprint 5 orienté profondeur runtime, historique cron, actions supplémentaires et finition premium.
- Sprint 5 exécuté et validé : historique des runs cron branché dans l’UI (`openclaw cron runs`), composant `cron-run-history`, profondeur de diagnostic accrue dans Calendrier, consolidation de la couche runtime et du moteur d’alertes ; Mission Control a commencé à passer de cockpit actionnable à cockpit profond et mature.
- Sprint 6 exécuté et validé : approfondissement de la lecture runtime et de la surface cron, confirmation/lecture des commandes CLI utiles (`status`, `run`, `enable`, `disable`), montée de maturité produit sans refonte ; à ce stade le produit était déjà jugé très bon et presque final.
- Sprint 7 exécuté et validé : vrai runtime agent fin exploité depuis `openclaw status --json` (`sessionsCount`, `lastActiveAgeMs`, `lastModel`, `percentUsed`), ajout de `app/api/cron/toggle/route.ts` + bouton `ToggleCronButton`, pages Équipe et Bureau rendues vraiment actionnables (liens ciblés + création de tâche directe), alertes enrichies avec signal “contexte Elon presque saturé”, et polish premium supplémentaire.
- Sprint 8 exécuté et validé comme sprint de finition premium : panneau alertes plus haut de gamme (catégories, niveau critique/attention/info, compteur, hiérarchie plus nette), home réorganisée pour mettre l’attention avant l’information, polish final sur la présentation ; conclusion de travail prise avec Denis : Mission Control v1 peut être considéré comme terminé / quasi final.
- État final fonctionnel de Mission Control après les 8 sprints : `/home/jarvis/.openclaw/workspace/mission-control/`, interface en français, home utile, Tâches/Calendrier/Mémoire/Équipe/Bureau branchés sur de vraies données OpenClaw, actions cron (`run`, `enable`, `disable`) exposées, runtime agent lisible, alertes crédibles, navigation inter-modules propre.
- Incident important pendant les captures d’écran : une capture headless a montré une home “cassée” sans style ; diagnostic réel = le serveur sur `127.0.0.1:3005` servait encore un ancien build Next.js où le HTML pointait vers un vieux CSS (`7ff1...`) alors que le build actuel contenait `640b...`, ce qui provoquait un rendu HTML brut sans styles.
- Correctif appliqué pour le problème de rendu : kill du vieux process `next-server`, redémarrage propre du bon build sur `127.0.0.1:3005`, vérification explicite que le CSS actuel répondait bien en `200 OK`, puis nouvelle capture de la home stockée dans `mission-control/screens/home-fixed.png` et envoyée sur Telegram à Denis.
- Dossier de captures créé pour Mission Control : `/home/jarvis/.openclaw/workspace/mission-control/screens/` avec au moins `home.png`, `tasks.png`, `calendar.png`, `memory.png`, `team.png`, `office.png`, puis `home-fixed.png` pour la vraie home corrigée.
- Décision stratégique finale avec Denis : arrêter les gros sprints Mission Control à ce stade ; un éventuel Sprint 9 ne serait plus nécessaire pour la v1 et relèverait plutôt d’une v2 ou d’un raffinement optionnel.
